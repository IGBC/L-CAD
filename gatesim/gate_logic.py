class LogicGate:
	def __init__(self):
		self.state = False #output state of the gate
		self.updated = False #
		self.inputs = [] #gates this gate accepts input from
		self.outputs = [] #gates this gate outputs to

	#enum gateType 
	bool state = False
	bool updated = False
	def update():
		pass
	LogicGate[] children = []
	LogicGate[] parents = []